# Rust Crate Licenses (auto-generated)

This appendix lists every Rust crate transitively used by the `lenzu`
binary, grouped by license.  Generated by `cargo about generate` —
do not hand-edit; run `make notices` from the repo root to regenerate.

## Summary

{{#each overview}}
- **{{{name}}}** ({{count}} crate{{#unless (eq count 1)}}s{{/unless}})
{{/each}}

## Crates by License

{{#each licenses}}
### {{{name}}} ({{{id}}})

Used by:
{{#each used_by}}
- `{{{crate.name}}}` {{{crate.version}}}{{#if crate.repository}} — <{{{crate.repository}}}>{{/if}}
{{/each}}

<details><summary>License text</summary>

```
{{{text}}}
```

</details>

{{/each}}
